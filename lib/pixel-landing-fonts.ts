import { Fraunces } from "next/font/google";

/** Distinctive readable body on the pixel landing */
export const pixelBodyFont = Fraunces({
  subsets: ["latin"],
  weight: ["400", "500"],
  display: "swap",
});
